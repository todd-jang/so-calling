apiVersion: v1
kind: Service
metadata:
  name: archscript-service
spec:
  selector:
    app: archscript
    color: blue # Points to blue initially
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: archscript-ingress
  annotations:
    kubernetes.io/ingress.class: "alb"
    alb.ingress.kubernetes.io/scheme: internet-facing
spec:
  rules:
  - host: dashboard.archscript.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: archscript-service
            port:
              number: 80
