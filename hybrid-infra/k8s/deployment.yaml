apiVersion: apps/v1
kind: Deployment
metadata:
  name: archscript-dashboard-blue
  labels:
    app: archscript
    color: blue
spec:
  replicas: 3
  selector:
    matchLabels:
      app: archscript
      color: blue
  template:
    metadata:
      labels:
        app: archscript
        color: blue
    spec:
      containers:
      - name: dashboard
        image: ncr.ncloud.com/archscript/dashboard:latest
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: archscript-dashboard-green
  labels:
    app: archscript
    color: green
spec:
  replicas: 0 # Scaled down initially
  selector:
    matchLabels:
      app: archscript
      color: green
  template:
    metadata:
      labels:
        app: archscript
        color: green
    spec:
      containers:
      - name: dashboard
        image: ncr.ncloud.com/archscript/dashboard:v2
        ports:
        - containerPort: 3000
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
